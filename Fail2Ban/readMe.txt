My code uses a scanner that reads the input file. It first looks for which linse have an invalid status and records
those IP addresses into an arraylist. It then iterates through that arraylist and keeps track of if an IP address
came up multiple times, keeping count. If that count is greater than or equal to 3, then it is recorded and printed out
to the output file.